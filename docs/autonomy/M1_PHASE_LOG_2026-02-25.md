# M1 Phase Log - 2026-02-25

## Phase
M1.1 - Goal-Hierarchie Fundament (Schema + Queue/Runner Hook)

## Geaenderte Dateien
1. `orchestration/task_queue.py`
2. `orchestration/autonomous_runner.py`
3. `tests/test_m1_goal_hierarchy.py`
4. `docs/autonomy/M1_P1_GOAL_HIERARCHIE.md`

## Kurzbeschreibung
1. Additive SQLite-Erweiterung um `goals`, `goal_edges`, `goal_state`.
2. Optionale Task-Verknuepfung per `tasks.goal_id`.
3. Feature-Flag gesteuerte Auto-Zielzuordnung in `TaskQueue.add(...)`.
4. Goal-Fortschrittsupdate nach Task-Ausfuehrung im `AutonomousRunner`.

## Test-Gates
Gate A:
1. `pytest -q tests/test_m1_goal_hierarchy.py` -> 6 passed
2. `pytest -q tests/test_m0_autonomy_contracts.py` -> 5 passed

Gate B + C:
1. `pytest -q tests/test_milestone5_quality_gates.py tests/test_milestone6_e2e_readiness.py`
2. Ergebnis -> 14 passed, 2 warnings (jsonrpcserver/importlib.resources deprecation)

## Kompatibilitaet
1. Kein Breaking Change an Kernsignaturen.
2. Bestehende TaskQueue-Aufrufe funktionieren unveraendert.
3. Neue M1-Logik nur bei aktivem Autonomie-Flag.

## Entscheidung
Go fuer M1.1.

## Naechster Schritt (M1.2)
Goal-Generator aus Memory/Curiosity/Event-Signalen anbinden und mit dedizierten
Tests gegen False-Positive-Ziele absichern.

---

## Phase
M1.2 - Signalbasierter Goal-Generator (Memory + Curiosity + Events)

## Geaenderte Dateien
1. `orchestration/task_queue.py`
2. `orchestration/goal_generator.py`
3. `orchestration/autonomous_runner.py`
4. `tests/test_m1_goal_generator.py`
5. `docs/autonomy/M1_P2_GOAL_GENERATOR_SIGNALS.md`

## Kurzbeschreibung
1. Neue Queue-Methoden fuer Signaleinbindung:
   - `upsert_goal_from_signal(...)`
   - `assign_task_goal(...)`
   - `get_unassigned_triggered_tasks(...)`
2. Neuer `GoalGenerator` mit drei Signalquellen:
   - Memory-Dialogzustand
   - Curiosity-Historie (`curiosity_sent`)
   - unzugeordnete Event-Tasks (`task_type=triggered`)
3. Heartbeat-Integration im `AutonomousRunner`:
   - Initialisierung bei aktivem M1-Flag
   - `run_cycle(max_goals=3)` bei jedem Wake-up

## Test-Gates
Gate A:
1. `pytest -q tests/test_m1_goal_generator.py` -> 5 passed
2. `pytest -q tests/test_m1_goal_hierarchy.py` -> 6 passed
3. `pytest -q tests/test_m0_autonomy_contracts.py` -> 5 passed

Gate B + C:
1. `pytest -q tests/test_milestone5_quality_gates.py tests/test_milestone6_e2e_readiness.py`
2. Ergebnis -> 14 passed, 2 warnings (jsonrpcserver/importlib.resources deprecation)

## Kompatibilitaet
1. Vollstaendig feature-flag-gated (`AUTONOMY_GOALS_ENABLED`, `AUTONOMY_COMPAT_MODE`).
2. Keine Breaking-Aenderungen an Dispatcher-/Queue-/Runner-Signaturen.
3. Nur additive Erweiterungen.

## Entscheidung
Go fuer M1.2.

## Naechster Schritt (M1.3)
Goal-Lifecycle haerten:
1. formale Status-Transitionen (`active`/`blocked`/`completed`),
2. Conflict-Erkennung zwischen Goals,
3. KPI-Exports (Goal-Alignment-Rate) fuer Monitoring/Canvas.

---

## Phase
M1.3 - Formale Goal-Lifecycle + Konflikt-Detektion + KPI-Export

## Geaenderte Dateien
1. `orchestration/task_queue.py`
2. `orchestration/autonomous_runner.py`
3. `gateway/telegram_gateway.py`
4. `main_dispatcher.py`
5. `tests/test_m1_goal_lifecycle_kpi.py`
6. `docs/autonomy/M1_P3_GOAL_LIFECYCLE_KPI.md`

## Kurzbeschreibung
1. Formale Status-Transitionen eingefuehrt (`transition_goal_status`).
2. Konflikterkennung und Konflikt-Sync eingefuehrt:
   - `detect_goal_conflicts`
   - `sync_goal_conflicts`
3. Goal-Alignment-KPIs eingefuehrt (`get_goal_alignment_metrics`).
4. KPI-Ausgabe integriert:
   - Telegram `/tasks` und `/status`
   - CLI-`/tasks` im Dispatcher
   - Heartbeat-Export als Canvas-Event `goal_kpi`

## Test-Gates
Gate A:
1. `pytest -q tests/test_m1_goal_lifecycle_kpi.py` -> 6 passed
2. `pytest -q tests/test_m1_goal_hierarchy.py tests/test_m1_goal_generator.py` -> 11 passed
3. `pytest -q tests/test_m0_autonomy_contracts.py` -> 5 passed

Gate B + C:
1. `pytest -q tests/test_milestone5_quality_gates.py tests/test_milestone6_e2e_readiness.py`
2. Ergebnis -> 14 passed, 2 warnings (jsonrpcserver/importlib.resources deprecation)

## Kompatibilitaet
1. Kein Breaking Change bestehender Kernsignaturen.
2. Goal-Erweiterungen bleiben additive Pfade.
3. M0-Contracts und bestehende Regression-Gates bleiben gruen.

## Entscheidung
Go fuer M1.3.
