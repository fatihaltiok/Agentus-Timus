#!/usr/bin/env python3
import os
import subprocess
import sys

root = os.path.dirname(os.path.dirname(__file__))
script = os.path.join(root, "scripts", "verify_pre_commit_lean.py")
sys.exit(subprocess.run([sys.executable, script], cwd=root).returncode)
